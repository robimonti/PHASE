[pytest]
testpaths = tests
markers =
    windows_only: test runs only on Windows (skipped elsewhere)
    requires_matlab: test requires a MATLAB installation on PATH
    nightly: long-running or nightly-only test
